/** @type {import('next').NextConfig} */
const nextConfig = {
    // experimental: {
    //     serverActions: true,
    //     serverActionsBodySizeLimit: '50mb',

    //   },
}

module.exports = nextConfig
